class Solution {
    public int[] getConcatenation(int[] nums) {
        int a[]=new int[nums.length*2];
        int k=0;
        for(int i=0;i<a.length;i++)
        {
            a[i]=nums[k++];
            if(k==nums.length)
                k=0;
        }
        return a;
    }
}
